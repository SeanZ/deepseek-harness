# my43 rc.2 升级

## 范围与基线

用户授权检查并升级 my43。生产起点为 rc.1 制品 `2c122992`；dev 起点 `094956c220`，工作树干净。锁定官方 `dsh-v0.1.5-rc.2` / `fb2c4b9e698e30edb738bca4cf0618587db7d203`。fork master `c291e7961a` 还含其它后续开发变更，不以版本字段相同作为正式发布代码相同的证据；本地 master 可快进，dev 仅合并官方标签并保留补丁历史。

rc.2 相比 rc.1 仅两笔非 merge 提交，包含反馈交互、文件卡片/图标和对应测试、版本号。未改请求注入、会话格式、模型适配器和鉴权核心。线上默认模型当前为 `deepseek-official/deepseek-v4-pro`、low，完整 settings 保留，禁止沿用昨天实验模型配置覆盖它。

## 分阶段计划

- [x] 核实正式版本范围、线上状态、模型与插件安装。
- [x] 合并，完成相关测试、构建、文档及发布检查。
- [ ] 本地打包上传，独立 Linux home/端口验证真实请求、注入、侧栏、原生依赖和近期会话副本。
- [ ] 无活跃运行时停服备份，切换 release 和 profile，验证鉴权、原数据与服务稳定。
- [ ] 提交推送 dev、更新运行约束、清理测试进程。
- [ ] 用户刷新页面人工验收后归档。

Planning Tool 未提供，以本文维护阶段状态。复用 rc.1 的本地制品与远端依赖安装流程，不在4GB服务器构建源码；安装限制内存和swap，校验所有本地包字节。保留三个插件版本及两份自定义补丁，隔离 home 不复制定时任务 store。Caddy、Authelia、systemd和启动引导不改动；切换失败恢复旧 release/profile，完整 home 快照保留，回退前保护新增数据。

本地28文件515项相关测试、SDK注入快照、插件10项测试、Python SDK fixture通过；完整构建、lint、34项doc-sync与16项hygiene通过。Python首次与文档门禁的Host重建并发，CLI临时缺失导致启动失败；门禁结束后重跑通过，后续已构建产物测试必须与重建串行。实际页面确认点击评分先开弹窗（未提交反馈）、Markdown预览和二进制下载正常，下载边界与PTY执行/重连实测通过，页面无错误。
