# my43 017rc2 升级

## 范围与基线

用户授权拉取并升级。沿用 dev、保留定制请求注入与 creative/unrestricted，维持 Caddy/Authelia 和生产数据；本地构建、服务器仅安装依赖。已有手机调查与 e969 修复文档保留。当前 dev e75363ce3031，生产 20260923-017rc1；目标官方 dsh-v0.1.7-rc.2 / 477b4f420553e8a52c2fbccc464d7561b239c443，与当前 origin/master、upstream/master 一致。

## 阶段与验收

- [x] fetch 官方与 fork，检查当前生产版本、资源、活动任务及差异。
- [x] 合并 rc2，语义迁移注入与 creative，处理冲突并验证核心/插件/历史。
- [ ] 本地构建、发布制品、独立 home/端口运行；验证 DeepSeek/Gemini 历史、真实连续工具、插件、Team、Office/终端与认证。
- [ ] 上传 Linux 安装、独立 canary 及包文件校验。
- [ ] 无活动任务后完整备份，切换 release/profile，验证生产配置、历史数据和鉴权。
- [ ] commit/push dev，清理本轮隔离进程；待用户人工验收再归档。

## 初始风险

rc1 到 rc2 跨 3429 个文件，包含动态工具更新、持久事件、模型选择与插件设置 UI、LibreOffice Kit 0.1.1。不能因补丁自动合并而跳过语义验证。预检冲突涉及 agent-loop、session、LLM 类型和生成文档/翻译记录。

当前 e969 历史定点维修已保留，升级必须使用最新生产快照，不得拿旧诊断副本覆盖。rc2 的 DeepSeek assistant 序列化仍保留空 text，不能宣称上游已修复；先确保已修会话正常，通用修复另行评估。

## 隔离与回退

本轮证据目录 .artifacts/017-rc2，权限0700。拟用本地43842、远端3082及独立canary home；上线前确认端口无人占用。release 为 /home/ubuntu/dsh-releases/20260925-017rc2，旧 release 20260923-017rc1 保留。生产停服后备份完整home与服务配置；切换失败先保全新home再恢复一致备份和旧链接。恢复检查必须包括配置、凭据、源会话以及浏览器认证，不能只看systemd active。

## 已发现的配置迁移

rc2把DeepSeek API key运行插件拆成 `@deepseek-ai/dsh-llm-deepseek-api-key`，旧`llm-deepseek`只保留协议实现。生产profile补丁带旧name，直接启动rc2时原模型列表覆盖未应用，退回官方默认。必须仅替换该行name，保留id、全部models和其它配置；隔离实例验证迁移后与原设置一致再部署。原agent-preset-registry.modeSelectionEnabled字段删除，由默认已开启的ui-settings代码工作工具控制；仅从预期设置中移除这个已废弃字段，其余值保持。

## 本地验证

合并后核心相关51文件1091测试、动态工具与注入回归9测试通过；SDK请求注入快照通过，更新内容仅为上游工具说明变化与新的DeepSeek插件名。全量build、lint通过；doc-sync两个问题（新增测试类型、旧翻译记录格式）修正后对应检查通过，83代码块及1135翻译对通过。近期9会话131源文件及旧格式9会话73源文件回读通过，包含已修复的e969，源文件不变。插件开关和刷新保持通过。GLM与DeepSeek分别完成真实六步工具调用，每次均只有一份system和injection，且连续请求不重复记录。
